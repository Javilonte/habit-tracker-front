import React from 'react';
import { View, Text } from 'react-native';

const Profile = () => {
    return (
        <View className="flex-1 justify-center items-center">
            <Text className="text-lg">Profilesss</Text>
            {/* Agrega componentes para mostrar la información del usuario, estadísticas, etc. */}
        </View>
    );
};

export default Profile;